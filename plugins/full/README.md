# full — Naga meta-plugin

One install pulls in all six Naga sub-plugins via dependency resolution: `naga-observe`, `naga-shift`, `naga-validate`, `naga-cross-repo`, `naga-fingerprint`, `naga-learning`.

```
/plugin install full@naga
```
