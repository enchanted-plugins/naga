# naga-learning

Persists the per-(pattern-class x target-domain) fidelity posterior on PreCompact via N5 Gauss Accumulation. The **single hook** in Naga — every other sub-plugin is skill-invoked. This 1-hook design is intentional and mirrors Wixie's skill-invoked lifecycle.
